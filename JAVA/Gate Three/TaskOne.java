public class TaskOne {
function TaskOne(arr, length, defaultValue = -1) {
  return arr.concat(Array(length - arr.length).fill(defaultValue));
     }
}
                                                                                                                                                                                                                             


